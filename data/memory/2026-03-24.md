
## 2:36 PM
- Reworked `src/lib/data-store.ts` to respect `DATA_REPO_ROOT`, normalize relative paths, and build GitHub URLs with the correct `data/` prefix so Vercel can see the JSON/Markdown assets; `npm run build` now passes again after the refactor.
- Next steps remain: verify the dashboards read real data in dev, stage/commit the data-store changes plus `data/` assets, push to `pnheimberg-svg/mission-control-dashboard`, redeploy to Vercel, and smoke-test live Kanban/Morning Momentum before reporting back to Paul.
